# 月度热点功能清理清单

## 📋 待清理文件清单

### 一、前端文件（9个文件）

#### 1. 页面组件
- ✅ `frontend-vue/src/views/MonthlyHotspotsView.vue` - 月度热点主页面

#### 2. 子组件（4个）
- ✅ `frontend-vue/src/components/hotspots/SectorOverview.vue` - 板块概览组件
- ✅ `frontend-vue/src/components/hotspots/SectorLeaders.vue` - 板块龙头组件
- ✅ `frontend-vue/src/components/hotspots/SectorEvents.vue` - 板块事件组件
- ✅ `frontend-vue/src/components/hotspots/SectorETFs.vue` - 板块ETF组件

#### 3. 配置文件（需要修改，不删除）
- ⚠️ `frontend-vue/src/components/layout/AppHeader.vue` - 删除菜单项（第75行）
- ⚠️ `frontend-vue/src/main.js` - 删除路由配置（第9行）
- ⚠️ `frontend-vue/src/services/dataCache.js` - 删除缓存键（第110行）
- ⚠️ `frontend-vue/src/api/stockApi.js` - 删除API方法（第94-140行，5个方法）

---

### 二、后端文件（1个主要文件 + 相关服务）

#### 1. API路由文件
- ✅ `backend/api/monthly_hotspots.py` - 月度热点API（/api/hotspots/*）

#### 2. 服务文件（需要确认是否被其他功能使用）
- ⚠️ `backend/services/hotspots/sector_heat_calculator.py` - 板块热度计算服务
  - **注意**：被 `update_sector_heat_snapshot.py` 脚本使用，需要确认该脚本是否也需要删除

#### 3. 配置文件（需要修改，不删除）
- ⚠️ `backend/app.py` - 删除导入（第24行）和路由注册（第102行）

---

### 三、数据更新脚本（需要确认）

- ⚠️ `backend/scripts/data_update/update_sector_heat_snapshot.py` - 更新板块热度快照
  - **注意**：如果删除月度热点，这个脚本可能也需要删除或修改

---

### 四、文档文件（3个）

- ✅ `docs/sector/BE_monthly_hotspots.md` - 月度热点后端设计文档
- ✅ `docs/sector/BE_monthly_hotspots_implementation_guide.md` - 月度热点实现指南
- ✅ `docs/月度热点功能清理清单.md` - 本文件（清理完成后可删除）

---

### 五、数据库表（需要确认是否被其他功能使用）

以下表可能被月度热点功能使用，但需要确认是否也被其他功能使用：

- ⚠️ `dim_hotspot_window` - 热点时间窗口表
- ⚠️ `fact_sector_heat_snapshot` - 板块热度快照表
- ⚠️ `fact_sector_event` - 板块事件表
- ⚠️ `dim_sector_etf_mapping` - 板块ETF映射表
- ⚠️ `fact_sector_etf_snapshot` - 板块ETF快照表

**建议**：先不删除数据库表，只删除代码。如果确认这些表没有被其他功能使用，再考虑删除。

---

### 六、配置文件（需要确认）

- ⚠️ `config/monthly_themes.json` - 月度主题配置（如果与月度热点相关）
- ⚠️ `config/monthly_theme.yaml` - 月度主题配置（如果与月度热点相关）

**注意**：`monthly_themes.py` API 看起来是独立的"月度主题"功能，不是"月度热点"，所以**不删除**。

---

## ⚠️ 注意事项

1. **热点簇功能（hotspot_cluster）**：这是独立功能，**不删除**
2. **月度主题功能（monthly_themes）**：这是独立功能，**不删除**
3. **数据库表**：建议先保留，确认无其他功能使用后再删除
4. **sector_heat_calculator服务**：需要确认是否被其他策略使用

---

## ✅ 清理步骤建议

1. **第一步**：删除前端页面和组件（9个文件）
2. **第二步**：修改前端配置文件（删除菜单、路由、API调用）
3. **第三步**：删除后端API文件（monthly_hotspots.py）
4. **第四步**：修改后端app.py（删除导入和路由注册）
5. **第五步**：删除文档文件
6. **第六步**：确认并删除数据更新脚本（如果需要）
7. **第七步**：确认并删除服务文件（如果只被月度热点使用）

---

## 🔍 需要确认的问题

1. `sector_heat_calculator.py` 是否被其他功能使用？
2. `update_sector_heat_snapshot.py` 脚本是否还需要？
3. 数据库表是否被其他功能使用？
4. `monthly_themes.json` 和 `monthly_theme.yaml` 是否与月度热点相关？
