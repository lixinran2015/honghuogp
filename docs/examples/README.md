# API 调用示例

`api_call_example.py` - 演示如何调用量化交易系统 API 获取数据。

运行前需启动后端（`start_backend.bat` 或 `python backend/run.py`），然后：

```bash
# 从项目根目录执行
python docs/examples/api_call_example.py
```

或指定参数查看帮助：`python docs/examples/api_call_example.py --help`
