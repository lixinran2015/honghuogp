# 🗄️ 数据库初始化说明

## ✅ 问题已解决

你遇到的错误 `relation "fact_daily_price" does not exist` 是因为数据库表还没有创建。

**现在已经解决了！** 所有数据库表已经成功创建。

## 📋 已创建的表

数据库初始化后，已创建以下表：

### 维度表（Dim）
- `dim_stock` - 股票维度表
- `dim_trade_calendar` - 交易日历
- `dim_sector` - 行业/板块维度
- `dim_sector_rotation_config` - 板块轮动配置
- `dim_hotspot_cluster` - 热点聚类
- `dim_hotspot_window` - 热点窗口

### 事实表（Fact）
- `fact_daily_price` - 日线价格数据（标准，多源合并）
- `fact_daily_price_qfq` - 前复权日线价格
- `fact_fundamental` - 财务数据（标准，多源合并）
- `fact_daily_fundamental` - 每日财务指标（PE、PB、ROE等）
- `fact_intraday_price_1m` - 分钟级分时数据
- `fact_limit_up_daily` - 每日涨停板数据
- `fact_market_emotion_daily` - 市场情绪日统计
- `fact_sector_daily` - 板块指数日线
- `fact_stock_sector` - 股票-板块关联
- `fact_event_driven_hotspot` - 事件驱动热点
- `fact_stock_snapshot` - 股票快照
- `fact_sector_heat_snapshot` - 板块热度快照
- `fact_sector_leader_snapshot` - 板块龙头快照
- `fact_hotspot_cluster_snapshot` - 热点聚类快照
- `fact_recommendation_result` - 推荐结果
- `fact_user_holding` - 用户持仓

### 原始数据表（Raw）
- `raw_daily_price` - 日线价格原始数据（多数据源）
- `raw_fundamental` - 财务数据原始数据（多数据源）

### 日志表
- `etl_log` - ETL执行日志
- `task_execution_log` - 任务执行日志

## 🚀 如何初始化数据库

### 方法1: 使用批处理脚本（Windows）

```powershell
.\init_database.bat
```

### 方法2: 使用命令行

```powershell
# 激活虚拟环境
.\venv\Scripts\Activate.ps1

# 运行初始化脚本
python -m data_warehouse.db_init
```

### 方法3: 使用Python模块

```python
from data_warehouse.db_init import init_database
init_database()
```

## 📝 初始化后的下一步

### 1. 初始化股票维表

```powershell
python -m data_warehouse.etl.init_stock_dim
```

这将从数据源获取所有A股股票的基本信息并填充到 `dim_stock` 表。

### 2. 回补历史数据（可选）

#### 样本回补（验证流程）

```powershell
python -m data_warehouse.etl.backfill_sample
```

回补3只样本股票（600519.SH, 000001.SZ, 300750.SZ）的1年数据。

#### 批量回补

```powershell
# 回补前100只股票（1年数据）
python -m data_warehouse.etl.backfill_batch --limit 100

# 回补指定股票
python -m data_warehouse.etl.backfill_batch --codes 600519.SH 000001.SZ --start 2024-01-01 --end 2025-11-16
```

#### 回补财务数据

```powershell
# 回补前50只股票的财务数据
python -m data_warehouse.etl.backfill_fundamental --limit 50
```

## ✅ 验证数据库

### 检查表是否创建

```powershell
python -c "from sqlalchemy import create_engine, inspect; from data_warehouse.config import DATABASE_URL; engine = create_engine(DATABASE_URL); inspector = inspect(engine); print('表数量:', len(inspector.get_table_names()))"
```

### 检查数据量

```powershell
# 使用psql
psql -U postgres -d quantitative_trading -c "SELECT 'dim_stock' as table_name, COUNT(*) as count FROM dim_stock UNION ALL SELECT 'fact_daily_price', COUNT(*) FROM fact_daily_price;"
```

### 测试连接

```powershell
python -c "from sqlalchemy import create_engine; from data_warehouse.config import DATABASE_URL; engine = create_engine(DATABASE_URL); conn = engine.connect(); print('✅ 连接成功！'); conn.close()"
```

## ⚠️ 常见问题

### 问题1: 表已存在错误

如果表已经存在，初始化脚本会跳过创建，这是正常的。

### 问题2: 索引创建失败

如果看到索引创建失败的警告，可以忽略。表结构已经创建成功，索引可以在后续手动创建。

### 问题3: 连接失败

确保：
1. PostgreSQL服务正在运行
2. 数据库 `quantitative_trading` 已创建
3. 用户名和密码配置正确（在 `data_warehouse/config.py` 中）

### 问题4: 权限不足

确保数据库用户有创建表的权限：

```sql
-- 在psql中执行
GRANT ALL PRIVILEGES ON DATABASE quantitative_trading TO postgres;
```

## 📚 相关文档

- 数据库配置: `数据库配置说明.md`
- PostgreSQL安装: `PostgreSQL安装指南_Windows.md`
- 数据仓库文档: `data_warehouse/README.md`

---

**提示**: 数据库表结构初始化完成后，就可以正常使用系统了。如果遇到其他错误，请检查数据库连接配置。

